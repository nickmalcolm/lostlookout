<h1>Listing Categories</h1>

<table>
  <tr>
    <th>Title</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @categories.each do |listing_category| %>
  <tr>
    <td><%= listing_category.title %></td>
    <td><%= link_to 'Show', listing_category %> </td>
    <td><%= link_to 'Edit', edit_listing_category_path(listing_category) %> </td>
    <td><%= link_to 'Destroy', listing_category, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Category', new_listing_category_path %>
